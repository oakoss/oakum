---
oakum: none
---

Test-only: check's tag look becomes a required parameter with its own type instead of a channel any look could fill, the scope report's look list and its remote clause are both derived from the plan that runs, and status, ci pr-status and version share one discovery seam. Output is byte-identical. Nothing published changes.
