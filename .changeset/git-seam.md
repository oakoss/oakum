---
oakum: none
---

Test-only: the git fake answers at the spawn so Git::child's own guards are unit-tested, the migrate gate look sits behind the Git seam in its own module, and five unix-only process tests the seam covers are retired. Nothing published changes.
