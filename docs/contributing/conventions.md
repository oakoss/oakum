# Conventions

- Commit messages: `type(scope): summary`.
- One branch per reviewable unit: `<type>/okm-<id>-<theme>`, where the id is the bead or the parent bead the branch carries. Sizing, parents, and order: [task-tracking.md](task-tracking.md).
- Don't push or open PRs unless asked.
